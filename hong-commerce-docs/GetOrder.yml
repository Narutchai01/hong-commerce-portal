info:
  name: GetOrder
  type: http
  seq: 12

http:
  method: GET
  url: http://localhost:8080/api/v1/orders/:id
  params:
    - name: id
      value: ""
      type: path
  body:
    type: none
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
