info:
  name: GetOrdersByUser
  type: http
  seq: 13

http:
  method: GET
  url: http://localhost:8080/api/v1/orders/user/:userId
  params:
    - name: userId
      value: ""
      type: path
  body:
    type: none
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
