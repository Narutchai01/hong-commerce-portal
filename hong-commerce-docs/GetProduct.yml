info:
  name: GetProduct
  type: http
  seq: 20

http:
  method: GET
  url: http://localhost:8080/api/v1/catalog/products/:id
  params:
    - name: id
      value: ""
      type: path
  body:
    type: none
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
