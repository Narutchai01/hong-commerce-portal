info:
  name: CreateCategory
  type: http
  seq: 16

http:
  method: POST
  url: http://localhost:8080/api/v1/catalog/categories
  body:
    type: json
    data: |-
      {
        "name": "Electronics"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
