info:
  name: RegisterAdmin
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8080/api/v1/users/admin/register
  body:
    type: json
    data: |-
      {
        "email" : "admin@example.com",
        "password" : "securepassword123",
        "name" : "Admin User"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
