info:
  name: CreateInventoryItem
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:8080/api/v1/inventories/items
  body:
    type: json
    data: |-
      {
        "name": "Sony PlayStation 5",
        "quantity": 15
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
