info:
  name: ListCategories
  type: http
  seq: 14

http:
  method: GET
  url: http://localhost:8080/api/v1/catalog/categories
  body:
    type: none
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
